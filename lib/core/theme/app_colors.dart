import 'package:flutter/material.dart';

class AppColors {
  // Primary — violeta/índigo del diseño
  static const Color primary = Color(0xFF7B7BFF);
  static const Color primaryDark = Color(0xFF5A5AE0);
  static const Color primaryLight = Color(0xFF9A9AFF);

  // Secondary — violeta botones
  static const Color secondary = Color(0xFF6B5EF8);
  static const Color secondaryDark = Color(0xFF5148E0);
  static const Color secondaryLight = Color(0xFF8B80FA);

  // Backgrounds
  static const Color background = Color(0xFF0A0B1A);
  static const Color surface = Color(0xFF13132E);
  static const Color surfaceElevated = Color(0xFF1C1C3A);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8888AA);
  static const Color textTertiary = Color(0xFF555577);

  // Status
  static const Color success = Color(0xFF34C759);
  static const Color error = Color(0xFFFF3B30);
  static const Color warning = Color(0xFFFF9500);
  static const Color info = Color(0xFF007AFF);

  // Borders
  static const Color border = Color(0xFF2A2A4A);
  static const Color divider = Color(0xFF1C1C3A);

  // Glass
  static const Color glassBackground = Color(0x1AFFFFFF);
  static const Color glassBorder = Color(0x33FFFFFF);
}
